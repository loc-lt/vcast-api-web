Insert_Bavia_History
---
tags:
  - Insert_Bavia_History
parameters:
  - name: body
    description: The body should contain the insert data
    in: body
    required: true
    schema:
      type: object
      required:
        - "Worker_Code"
        - "Bavia"
        - "Quantity"
      properties:
        Worker_Code:
          type: "Worker_Code"
          example: "XXXXXXXX"
        Bavia:
          type: "Bavia"
          example: "XXXXXXXX"
        Quantity:
          type: "Quantity"
          example: "XXXXXXXX"
responses:
  201:
    description: When a data insert success

  400:
    description: Fails to insert due to bad request data
